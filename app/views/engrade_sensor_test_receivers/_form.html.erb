<%= form_for(@engrade_sensor_test_receiver) do |f| %>
  <% if @engrade_sensor_test_receiver.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@engrade_sensor_test_receiver.errors.count, "error") %> prohibited this engrade_sensor_test_receiver from being saved:</h2>

      <ul>
      <% @engrade_sensor_test_receiver.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :headers %><br />
    <%= f.text_field :headers %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
